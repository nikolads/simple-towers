(
  axes: {
    "camera_translate_x": Emulated(pos: Key(D), neg: Key(A)),
    "camera_translate_z": Emulated(pos: Key(S), neg: Key(W)),
    "camera_zoom":        Emulated(pos: Key(E), neg: Key(Q)),
    // rotate the camera around the up axis
    "camera_pitch":       Emulated(pos: Key(L), neg: Key(J)),
    // rotate the up axis of the camera up or down
    "camera_yaw":         Emulated(pos: Key(K), neg: Key(I)),
  },
  actions: {
    "spawn_enemy": [Key(O)],
  },
)
